class Meals {
  constructor(id, dishName, price, isVeg, img, discount) {
    this.id = id;
    this.dishName = dishName;
    this.price = price;
    this.isVeg = isVeg;
    this.img = img;
    this.discount = discount;
  }
}

export default Meals;
